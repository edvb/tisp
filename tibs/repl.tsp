(define (repl)
  (print "> ")
  (let ((expr (read)))
    (unless (and (pair? expr) (= (car expr) 'quit))
      (let ((ans (eval expr)))
        (unless (void? ans)
          (disp-string ans)))
      (repl))))

(repl)
